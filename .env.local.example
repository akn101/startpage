# API key for webhook callers (Android via Tasker, openclaw AI, etc.)
# Generate with: openssl rand -hex 32
NOTIFY_API_KEY=your_secret_key_here

# Required by next-auth (can be any random string for now)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000
